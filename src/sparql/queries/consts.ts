/* Geoconnex ontology terms (water/geospatial semantic vocabulary) */
export const GCX = 'gcx';
export const GCX_PREFIX = `PREFIX ${GCX}: <https://geoconnex.us/ontologies/>`;

/* GIS GeoSPARQL query ontology */
export const GEO = 'geo';
export const GEO_PREFIX = `PREFIX ${GEO}: <http://www.opengis.net/ont/geosparql#>`;

/* HydroSHEDS HYBAS (hydrologic basin identifiers) */
export const HYB = 'hyb';
export const HYB_PREFIX = `PREFIX ${HYB}: <https://geoconnex.us/hydrosheds/hybas/>`;

/* Observation resources (e.g., environmental flow or sensor observations) */
export const OBS = 'obs';
export const OBS_PREFIX =
    'PREFIX ${OBS}: <https://geoconnex.us/observations/efr_>';

/* Quantities, units, dimensions, and data types ontology */
export const QUDT = 'qudt';
export const QUDT_PREFIX = `PREFIX ${QUDT}: <http://qudt.org/schema/qudt/>`;

/* Core RDF syntax vocabulary */
export const RDF = 'rdf';
export const RDF_PREFIX = `PREFIX ${RDF}: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>`;

/* RDF schema vocabulary (classes, labels, hierarchies) */
export const RDFS = 'rdfs';
export const RDFS_PREFIX = `PREFIX ${RDFS}: <http://www.w3.org/2000/01/rdf-schema#>`;

/* Schema.org structured data vocabulary (web entities, metadata) */
export const SCHEMA = 'schema';
export const SCHEMA_PREFIX = `PREFIX ${SCHEMA}: <https://schema.org/>`;

/* Simple Knowledge Organization System (SKOS) vocabulary */
export const SKOS = 'skos';
export const SKOS_PREFIX = `PREFIX ${SKOS}: <http://www.w3.org/2004/02/skos/core#>`;

/* Sensor, observation, sample, and actuator ontology (W3C IoT/observations) */
export const SOSA = 'sosa';
export const SOSA_PREFIX = `PREFIX ${SOSA}: <http://www.w3.org/ns/sosa/>`;

/* XML schema datatypes (strings, numbers, dates) */
export const XSD = 'xsd';
export const XSD_PREFIX = `PREFIX ${XSD}: <http://www.w3.org/2001/XMLSchema#>`;

export const HYF = 'hyf';
export const HYF_PREFIX = `PREFIX ${HYF}: <https://www.opengis.net/def/schema/hy_features/hyf/>`;
